const { IssueModel } = require('./dbConn');

module.exports = function (app) {
    app.route('/api/issues/:project')
        .post((req, res) => {

        })

    app.route('/api/issues/:project')
        .put((req, res) => {

        })

    app.route('/api/issues/:project')
        .delete((req, res) => {

        })

    app.route('/api/issues/:project')
        .get((req, res) => {

        })
}